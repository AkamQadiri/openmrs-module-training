{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://openmrs.org/schemas/training/exercise-content-ordering.schema.json",
  "title": "Ordering Exercise Content",
  "type": "object",
  "required": ["instructions", "items"],
  "properties": {
    "instructions": { "$ref": "#/definitions/localizedText" },
    "items": {
      "type": "array",
      "minItems": 2,
      "items": {
        "type": "object",
        "required": ["id", "text"],
        "properties": {
          "id": { "type": "string", "pattern": "^[1-9][0-9]*$" },
          "text": { "$ref": "#/definitions/localizedText" }
        }
      }
    }
  },
  "definitions": {
    "localizedText": {
      "type": "object",
      "patternProperties": {
        "^[a-z]{2}(-[A-Z]{2})?$": { "type": "string" }
      },
      "required": ["en"],
      "additionalProperties": false
    }
  }
}
